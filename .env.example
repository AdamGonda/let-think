# Convex - populated automatically when you run `npx convex dev`
VITE_CONVEX_URL=

# Server-side (Convex Dashboard): GOOGLE_GENERATIVE_AI_API_KEY for Gemini 3.1 Pro Preview

# Add other client-safe env vars with VITE_ prefix, e.g.:
# VITE_APP_NAME=think
