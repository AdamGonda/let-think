{
  "name": "let-think",
  "user": "ubuntu",
  "install": "npm ci && CONVEX_AGENT_MODE=anonymous npx convex dev --once",
  "terminals": [
    {
      "name": "convex",
      "command": "CONVEX_AGENT_MODE=anonymous npx convex dev",
      "description": "Convex local backend (anonymous agent mode). Watches convex/ and serves the deployment referenced by VITE_CONVEX_URL in .env.local."
    },
    {
      "name": "vite",
      "command": "vite --host",
      "description": "Vite dev server for the React SPA on port 5173. Authenticated /app testing: follow docs/agent-local-login.md (see AGENTS.md)."
    }
  ],
  "ports": [
    {
      "name": "vite",
      "port": 5173
    }
  ]
}
